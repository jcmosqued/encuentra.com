function initMap() {
        // Create a map object and specify the DOM element for display.
        var map = new google.maps.Map(document.getElementById('mapaMiniatura'), {
          center: {lat: 21.12048674253761, lng: -101.67953196892546},
          zoom: 15
        });
}
